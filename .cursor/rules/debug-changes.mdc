---
description: 
alwaysApply: true
---

ALWAYS run the following checks after finishing code changes:
1. Linter: [npm run lint]
2. Build: [npm run build]
3. Docker: docker compose up -d --build

If checks fail, fix the errors automatically and retry. Do not consider the task done until the Docker image builds successfully.
